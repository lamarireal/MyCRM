# MyCRM Web

The React frontend built with TypeScript and Vite.

```powershell
pnpm install
pnpm dev
```

The development server is available at <http://localhost:5173> and proxies
`/api` requests to FastAPI.
